services:
  accoder:
    build:
      context: .
      dockerfile: Dockerfile.app
    image: accoder-app:local
    ports:
      - "43117:43117"
    volumes:
      - ./data/memory:/app/data/memory
    environment:
      PORT: "43117"
      ACCODER_HOST: "0.0.0.0"
